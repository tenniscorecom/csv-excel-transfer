# 顧客情報転記 — AI エージェント向け実装方針

このリポジトリ（`F:\dev\顧客情報転記`）は、西日本・東日本の顧客 CSV 2 本をマージし、
INPUT エクセルの「業務用ID」列をキーに複数列を転記したうえで、開封パスワード付きで
`最終_` プレフィックス付きで保存し、成功時に元ファイルを削除する業務ツールです。
正本のプロジェクトノートは [Projects/顧客情報転記.md](../Brain/Projects/顧客情報転記.md)
に置いています。**実装上の判断に迷ったら、まず現状のファイル構成と
[docs/仕様書.md](docs/仕様書.md) を確認してください。**

このドキュメントは「今後の変更で守るべき方針」をまとめたものです。現状の構成を
正解として扱い、現実に問題が出るまで抽象化・分割・層化を行わないことを最優先します。

---

## 結論

**現状は過剰なリファクタリングをしない。**

現在の規模では、さらにファイルやレイヤーを分割するよりも、業務フローを少ない
ファイルで追えることを優先します。AI エージェント（Codex / M3）からの「将来拡張の
ために分けたい」「役割ごとに切り出した方が綺麗」といった提案は、このドキュメントの
リファクタリング判断基準（後述）に照らして却下して構いません。

---

## 構成方針

**`main.py` → `src/run.py` → comken** の 3 段構造を維持してください。

### `main.py`

アプリケーションの入口として扱います。

責務は以下に限定してください。

- 初期設定
- 必須設定の検証
- `DRY_RUN` / `DEBUG` など実行環境の設定
- `run()` の呼び出し
- 最上位の例外処理

業務ロジックは増やさないでください。

### `src/run.py`

**現在の業務フローの中心として維持してください。**

「設定を読む → 出力先を決める → CSV をマージする → Excel へ転記する → 保存する →
成功後に元ファイルを削除する」という流れを、1 ファイルで上から追えることを優先して
ください。`merge.py` / `transfer.py` / `cleanup.py` / `output.py` のような機械的な
分割は不要です。**「機能ごとに分けられる」だけでは分割理由になりません。**

実際に `run.py` が長くなり、業務フローを理解できなくなった場合にのみ分割を検討
してください。

---

## comken との責務分離

複雑な汎用処理は可能な限り comken に寄せてください。

ただし、アプリケーション側で comken の内部仕様を再実装・再テストしすぎないように
してください。

アプリケーション側では「comken の内部でどう動くか」よりも、**「このアプリケーション
として成功・失敗時にどう振る舞うか」** をテストすることを優先してください。

> **dry-run の穴に注意。** comken に不可逆処理（保存・削除）を追加・変更した際は、
> dry-run が本当に効いているかを実際に確認してください。

---

## テスト方針

アプリケーションとして重要なケースは維持してください。

例えば、

- 正常に転記される
- 複数 CSV が正しく統合される
- 該当しないデータは変更されない
- 重複データで停止する
- 途中で失敗した場合、元ファイルを削除しない
- `DRY_RUN` では実ファイルを変更しない

などです。

一方で、comken 側で保証される詳細な内部アルゴリズムまで、このリポジトリで再検証
する必要はありません。

---

## リファクタリング判断基準

今後の変更では、以下を優先してください。

1. **現在のコードが分かりやすくなるか**
2. **実際に重複や複雑さが存在するか**
3. **変更後にファイルを追う手間が増えていないか**
4. **将来使うかもしれない抽象化ではなく、現在必要な抽象化か**

**現状の規模では「適度にまとまっていて、1 つの業務フローを少ないファイルで追える
こと」を優先してください。**

変更提案を行う場合も、まず「現状の問題点」を具体的に示し、その問題が実際に存在
する場合のみリファクタリングしてください。
